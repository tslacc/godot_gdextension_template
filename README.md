# godot_gdextension_template
A template for the C-based GDExtension for the Godot game engine.

Build by running make: this builds and outputs the final shared library to /tmp/build or wherever tmpdir is.
